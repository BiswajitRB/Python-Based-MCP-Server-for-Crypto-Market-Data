name: end to end pipline getways        #all devsecops pipline called here
on:
    push:
    workflow_dispatch:
jobs:
    docker-build-push:
        uses: ./.github/workflows/docker-build-push.yml
        secrets:
            DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USER }}
            DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
